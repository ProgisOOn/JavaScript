"use strict";

alert("JavaScripts has following comparison signs -->");
alert("> < >= <= == !=");
alert("The same in the python");
alert("result is bolean 4>2");
alert(4>2);

let strings;
strings="String are compared letter-by-letter if the first letter is greater than second";
strings="we stop if characters equal we compare seconds";
strings='if they not equal comparison stops'

alert('2'>1);

alert(true == 1);
alert(false == 0);

//fun moment

let a=0,
    b="0";
alert('we here');
alert(Boolean(a));
alert(Boolean(b));
alert(a==b);

//but when we want to differentiate 0 from false??

let tooStrict = '===';

alert(0 === false);

tooStrict = 'This operator do not convert values into number instead he immediately returns false';
tooStrict = 'if types are different';

alert("compare null with undefined");
alert(null === undefined);
alert("compare null with undefined with non-strict");
alert( null == undefined);

alert('null is greater than zero');
alert(null>0);
alert('null is less than zero');
alert(null<0);
alert('null equals zero');
alert(null == 0);
alert("null >= zero");
alert(null >= 0);

let rules;

rules = "undefined equals null and undefined only"
rules = "undefined is converted in NaN"
rules = 'special type which returns always false'

alert(null == 0);




